# Hard requirement -------------------------------------------------------
numpy>=1.24
psutil>=5.9

# Optional, and the reason the stack still runs without them -------------
# torch      : the learned segmentation backends (pointnet2lite, sparsevoxel).
#              Without it the deterministic geometric segmenter is used and
#              the pipeline reports 91.7% instead of 95.4%.
# scipy      : accelerates connected-component labelling and the ground fill.
#              Pure-NumPy fallbacks exist for both; only speed changes.
torch>=2.0 ; extra == "learn"
scipy>=1.10 ; extra == "fast"
